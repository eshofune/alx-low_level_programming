#include "main.h"
/**
* main - tests function that prints if integer is positive or negative
* positive_or_negative: prints either positive or negative number
* Return: Always 0 (Successful)
*/
int main(void)
{
int i;
i = 0;
positive_or_negative(i);
return (0);
}
